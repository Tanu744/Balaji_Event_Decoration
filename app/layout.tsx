import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Balaji Event & Decorations - Making Every Celebration Memorable",
  description:
    "Professional event decoration services for weddings, birthdays, corporate events, and all special occasions. Transform your celebrations with our creative designs.",
  keywords: "event decoration, wedding decoration, birthday party, corporate events, balloon decoration, Balaji events",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
